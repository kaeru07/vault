---
date: 2026-07-22
task: 麻雀トレーナーUI刷新を自動実行キューへ登録 + 麻雀問題アプリの未取り込み画像47枚を発見・変換基盤整備
runId: 20260722-004428
targetApp: ny01/mahjong-trainer
monetizationImpact: medium
theme: [mahjong, app-strategy, workflow]
relatedRunIds: []
commitHashes: [495b3d9, fc40cc4]
reviewFileCommit:
---

# 2026-07-22 麻雀トレーナーUI刷新の自動実行登録 / 麻雀問題アプリの画像取り込み調査

## 1. 作業目的

2つの依頼を扱った。

1. **麻雀トレーナー(ny01/mahjong-trainer)のUI根本刷新**。ユーザー要望は「牌の画像もフィールド全体のバランスも良くない。天鳳や雀魂のようなUIになること」。「この作業何回も上手くいってない」との明示があり、**同じ失敗を繰り返さない仕組み**が必要だった。途中で「次回自動実行で行うように置いておいて」と指示変更されたため、実装ではなくキュー登録が成果物になった。
2. **麻雀問題アプリ(apps/mahjong)への問題追加**。「過去スクリーンショットを大量に渡すので問題として追加して欲しい」。その後「未取り込みの画像あった？」と確認があった。

## 2. 実施内容

### 2-1. mahjong-trainer UI

- Playwright(chromium)で実描画し、390x844 / 375x667 / 844x390 の3画面を撮影して**目視診断**した
- 過去のUI改修3件(`0d2ff29` 河グリッド化 / `185605b` レスポンシブ / `4e14581` 牌デザイン)を確認。**毎回「buildが通った」で終わり、実描画を見ていない**のが失敗の共通原因と判断
- 検証スクリプト `scripts/screenshot.mjs` を新規作成。3画面のスクショ取得に加え、**牌の実座標がビューポート外に出ていないかを機械判定**して OK/NG を返す
- Goal `goal-mruorbo0-ylrq3`（麻雀読みトレーナーの卓UIを天鳳/雀魂水準にする）を作成し、完了条件を**9件のToDoへ分解**
- prompt-queue `pq-mrup6ppe-6kow50` として登録。`nextCandidates` order=1 を確認済み

### 2-2. 発見した現状UIの具体的不具合（実測）

| 症状 | 実測値 |
|---|---|
| 手牌が右で切れる | 390px幅で手牌14枚が right=551px（**161px超過**） |
| 対面の伏せ牌が左で切れる | 全牌の left=**-5px** |
| 375x667でも同様 | right=551px |
| 横持ち(844x390)のみ | はみ出しなし |

加えて目視で確認した点:
- 河が「CPU西の河」等の**テキストラベル付きで縦3ブロック**に並び、麻雀卓に見えない
- 卓が**縦長の長方形**（本来は正方形＋4河の風車配置）
- 牌絵柄が実牌と別物（筒子=ベタ塗りの青丸 / 萬子=serifフォント依存で iOS 実機で崩れる懸念）
- 配色が濃紺グレー＋蛍光紫ボタンでゲームらしくない

### 2-3. apps/mahjong の画像取り込み調査

- **未取り込み画像は「あった」。`screenshot/yomi/` に47枚**（IMG_8361〜8407）
- 中身は**市販ドリル本「手牌読み」第1章**を撮影したもの。問題ページと解答ページが交互。**約23問分**
- `inputdata/` + `converted/` の53枚は**全て取り込み済み**（q011〜q036 が `sourceImages` で参照）
- `screenshot/` 直下の54枚は **0バイト/10KB の破損ファイル**で読めない（転送失敗の残骸）
- HEIC変換環境が無かったので `/root/.venvs/heic` に pillow-heif を導入
- `scripts/prepare-images.py` を新規作成（走査 → JPEG正規化 → manifest → **未問題化画像の一覧**）。47枚を `converted/yomi/` へ変換済み
- 問題1を実際にズームして読み取り、**正確な転記が可能**なことを確認（既存26問が全て `needsReview:true` なのとは対照的）

## 3. 変更ファイル

| ファイル | 変更 |
|---|---|
| `apps/ny01/mahjong-trainer/scripts/screenshot.mjs` | 新規。実描画スクショ＋牌はみ出しの実座標判定 |
| `apps/mahjong/scripts/prepare-images.py` | 新規。画像走査→JPEG正規化→manifest→未問題化一覧 |

commit: `495b3d9`(ny01, pushed) / `fc40cc4`(mahjong, pushed to `ios-codemagic-test`)

## 4. 検証結果

- `scripts/screenshot.mjs` を実行し、3画面で撮影と判定が動作することを確認（現状は想定どおり **NG** を返す＝ベースラインとして機能する）
- 撮影画像を目視し、手牌と対面牌の切れを確認
- `prepare-images.py` を既存53枚に対して実行 → 「未問題化 0 件」を正しく検出
- 47枚のHEIC→JPEG変換が成功（1800x2400）
- prompt-queue GET で `nextCandidates` order=1 を確認
- UI改修自体は未着手のため lint/build は未実施

## 5. 未対応

- **47枚（約23問）の問題生成そのもの**。著作権の扱いが未判断のため着手していない
- mahjong-trainer のUI実装（次回自動実行に委譲）
- `screenshot/` 直下の破損54枚の削除（ユーザー判断待ち。inputdata/ に同名の正常ファイルがあり実害はない）

## 6. 危険ポイント

- **市販ドリル本の問題をストア配信アプリへ転記する著作権リスク**。既存26問(q011〜q036)は `book`/`chapter`/`sourcePage` 付きで既に取り込み済みであり、**同じ状態で App Store 配信されている**。47枚を追加すると同種の内容が約2倍になる。公開可否は法務判断であり承認必須事項
- `apps/mahjong` は **`ios-codemagic-test` ブランチで作業されており main が10コミット遅れ**。TestFlight版の実体はこのブランチ。main へ push しても製品に反映されない
- 生画像は `.gitignore` 済みで再配布はされていない（`converted/` `inputdata/` `screenshot/` `*.jpg` 等）

## 7. 次にやるべきこと

1. **ユーザー判断**: ドリル本由来の問題をストア配信アプリに載せてよいか。NGなら既存26問の扱いも要検討
2. OKなら47枚→約23問を生成（`needsReview` を極力出さない品質で）
3. 次回自動実行で `pq-mrup6ppe-6kow50` が実行される。結果のスクショ目視まで確認する
4. `apps/mahjong` のブランチ整理方針を決める

## 8. ChatGPT レビュー依頼文

```
麻雀アプリ2本について、Claude Codeの作業結果をレビューしてください。

対象: kaeru07/ny01 (commit 495b3d9) / kaeru07/mahjong (commit fc40cc4, branch ios-codemagic-test)
runId: 20260722-004428

やったこと:
1. ny01/mahjong-trainer のUI刷新を自動実行キューへ登録。過去3回失敗しているため、
   実描画スクショ＋牌のはみ出し実座標判定を行う検証スクリプトを先に用意し、
   完了条件を9個のToDoに分解した。
2. apps/mahjong で未取り込み画像47枚（市販ドリル本「手牌読み」の撮影、約23問分）を発見し、
   HEIC変換基盤を整備した。問題生成自体は著作権判断待ちで未着手。

見てほしい観点:
- 市販ドリル本の問題を、App Store配信アプリの問題データとして転記することの法的リスク。
  既に26問が同じ形で配信済み。取り下げるべきか、改変すれば足りるか、出典表記で足りるか。
- 「過去3回失敗したUI改修」に対して、検証スクリプト＋ToDo分解という対策で
  再発を防げるか。他に入れるべきゲートはあるか。
- apps/mahjong が ios-codemagic-test ブランチで進んでおり main が10コミット遅れている状態の
  整理方針（main へマージすべきか、ブランチを主軸に切り替えるべきか）。
```
