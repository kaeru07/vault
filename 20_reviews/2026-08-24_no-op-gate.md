---
date: 2026-08-24
task: 空振り（変更0かつ出力なし）の作業を完了扱いにしないゲートを追加
runId: 20260824-213636
targetApp: ny01-progress
monetizationImpact: low
theme: [workflow]
relatedRunIds: [20260823-110457, 20260824-191153]
commitHashes: [bc81977, c67db08]
reviewFileCommit:
---

## 作業目的

スライド資料（runId 20260823-110457）で挙げた「次の一手」の #3。

自動実行の実測で、**ファイル変更を伴った実行は51%**しかなく、「（出力なし）」で終わった実行が36件あった。
さらに 2026-08-22 の runId `20260822-160657-718` は `partial` / 「（出力なし）」の実質 no-op なのに、
作業予約（Prompt Queue）側は **completed** になっていた。回った回数と成果が一致しないと、
優先順位の判断材料が濁る。

## 実施内容

- `lib/no-op-run.ts` に `classifyNoOpRun()` を新設。**変更ファイル0件 かつ stdout が空 かつ 要約が空または「出力なし」系**のときだけ空振りと判定
- `failed` は空振り扱いにしない（失敗として従来どおり別経路で処理）
- **Factory 本体**（Run 記録2箇所）と **Prompt Queue** の両方で、空振りは `completed` にせず **partial** で記録し、warnings に理由を残す
- **作業予約は `completed` ではなく `needs_retry`** にして、次回の自動実行で再試行する
- 既存の lint ゲート（checks NG で completed→partial）と併存

## 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `lib/no-op-run.ts` | 新規。空振り判定 |
| `lib/no-op-run.test.ts` | 新規4テスト（実例の「（出力なし）」を含む） |
| `lib/factory-runner.ts` | Run 記録2箇所へゲート適用＋warnings |
| `lib/prompt-queue-runner.ts` | partial 記録＋作業予約を needs_retry へ |
| `docs/operations/current-operating-model.md` | 新セクション・変更履歴・frontmatter |

## 検証結果

- `npx tsc --noEmit`: OK
- `npm test`: **209件 全pass**（新規4件）
- `npm run build`: exit 0
- push: `kaeru07/ny01` `bc81977`

### 同時に入れた分（commit c67db08 / runId 20260824-191153）

- 「ヒットアプリ調査」をアプリ開発タブのサブタブへ追加（相互に行き来できるようサブタブバーも表示）
- 市場分析（`/app-market-analysis`）のスマホ縦長を解消。**54,471px（64.5画面分）→ 2,916px（3.5画面分）**。
  JS を足さず `details/summary` で、候補66件を要約1行カード化＋11件目以降はまとめて折りたたみ

## 未対応

- **既に completed で記録済みの過去 Run は遡って修正していない**（今後の実行から適用）
- 「空振りが何件あったか」を集計する画面は作っていない。warnings 文字列で追える状態まで

## 危険ポイント

- 判定を厳しくしすぎると、正常なのに出力を返さない executor の実行まで needs_retry で無限に再試行される可能性がある。
  現在の条件は「変更0 **かつ** 出力なし」と厳しめに絞ってあるが、再試行が積み上がっていないかは次回以降の自動実行で確認が要る
- Prompt Queue の作業予約が needs_retry で残るため、同種タスクの重複防止ロジック（例: ヒットアプリ調査の
  `ensureMarketResearchTask`）は「未完了扱い」として新規を積まない。詰まったときに気付けるよう、次回実行後の状態確認が必要

## 次にやるべきこと

1. 次回の自動実行で、空振りが `partial` / `needs_retry` として記録されることを確認する
2. needs_retry が滞留していないか（同じタスクが再試行され続けていないか）を見る
3. 残る「次の一手」: レビュー470件の棚卸し / 停止中の目標45件の仕分け / ストアへ1本出し切る

## ChatGPT レビュー依頼文

```
対象: kaeru07/ny01（main, commit bc81977）/ progress アプリ
runId: 20260824-213636

自動実行で「実質何もしていない実行」が完了扱いになっていた問題に、
機械的なゲートを入れました。以下の観点でレビューしてください。

1. 判定条件の妥当性
   - 「変更ファイル0件 かつ stdout空 かつ 要約が空または『出力なし』系」のときだけ空振り。
   - failed は対象外。厳しすぎ／緩すぎになっていませんか。
   - 調査タスクのように「ファイルを変更しないのが正常」な作業を誤って空振り判定するリスクは、
     POST を伴う作業（例: ヒットアプリ調査）ではどう扱うべきですか。

2. 再試行の設計
   - 作業予約を completed ではなく needs_retry に落として次回再試行します。
   - 同じタスクが延々と再試行される事態を防ぐには、試行回数の上限を持たせるべきですか。

3. 過去データ
   - 既に completed で記録済みの過去 Run は遡って直していません。
     集計の一貫性のために遡及修正すべきか、履歴は当時のまま残すべきか。
```
