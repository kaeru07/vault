---
date: 2026-07-19
task: iOSビルドを下タブへ追加+今日の判断カード(ビルド候補/危険判断)の人間語化
runId: 20260719-093122
targetApp: progress
monetizationImpact: low
theme: [workflow]
relatedRunIds: [20260718-232410]
commitHashes: [22c4c27]
# reviewFileCommit:
---

# 2026-07-19 iOSビルド下タブ追加+今日の判断カードの人間語化

## 1. 作業目的

- ユーザー指摘①「iOSビルド画面がどこにあるか分からない。全体のタブに追加して」②「暇つぶしアプリの今日の判断が分かりにくすぎる。詳細も人間向けでない。判断できる説明にして」

## 2. 実施内容

- 下タブ(BottomNav)5番目に「iOSビルド」追加+全画面メニュー掲載
- iOSビルド候補カード刷新: 見出し文章化・JST日時・「配信されていない変更」のコミット件名列挙・選択肢の効果説明。生hash/ISO日時を排除。既存未決カードは内容更新方式に変更
- 調査で判明: ユーザーの言う「暇つぶしの分かりにくいカード」はiOSビルド候補ではなく、**完了済みRun(7/11 認証確認+ステージ制化)をai-reviewが危険キーワードで起票した確認カード**。表示側が「実行しようとしています/許可しますか？」と未実行かのような誤解を招く文言だった
- 修正: ai-review起票を人間語化(問題なし/修正を依頼する/保留)+command-centerで完了済みRun由来は「完了した作業の確認が必要です」表示+詳細に「この作業でやったこと」「確認のポイント」
- メンテAPI(refresh-ai-review-danger)で既存カードを新形式へ更新済み

## 3. 変更ファイル

- BottomNav.tsx / nav-menu.ts / pickup route / lib/ios-builds.ts(+types/test) / ai-review.ts / command-center.ts / refresh-ai-review-danger route(新規)

## 4. 検証結果

- tsc / test 23件 / build OK。/decide headless実描画で両カードの新表示を目視確認(麻雀=未配信変更1件の件名と日時表示、hima=完了作業の確認+3択)
- コミット分離: buildInbox同一領域を前セッション未コミット変更と両方が触るため、混在4ファイルは前セッション変更(Inbox hold挙動・nav並び等、稼働中のもの)を同梱。indexツリーtscで整合確認後commit/push

## 5. 未対応

- 承認決定(ビルドする)→自動ビルド起動の連携は未実装(次フェーズ候補)

## 6. 危険ポイント

- ai-reviewの起票keyをmark_reviewed/needs_followupへ変更(既存decide APIが対応済みのkey)。旧カードのproceed/cancel keyはメンテAPIで新keyに更新済みのため取り残しなし

## 7. 次にやるべきこと

- iPhoneで下タブ「iOSビルド」表示と今日の判断2カードの文言を確認
- 判断してみて分かりにくければ文言フィードバック

## 8. ChatGPT レビュー依頼文

```
progressの「今日の判断」カード人間語化のレビューをお願いします。
runId: 20260719-093122 / commit: 22c4c27

内容: (1)iOSビルド候補カードを見出し文章化・JST日時・未配信コミット件名列挙に刷新 (2)完了済み作業を危険キーワードで起票した確認カードが「実行しようとしています/許可しますか？」と誤解を招いていたのを「完了した作業の確認が必要です」+「この作業でやったこと/確認のポイント」に修正 (3)iOSビルドを下タブに追加。

確認したい観点:
1. 完了済み作業の事後確認カードの文言設計(誤解の余地が残っていないか)
2. ai-review起票optionのkey変更(mark_reviewed等)の互換リスク
3. 前セッション未コミット変更を同梱したコミット判断の妥当性
```
