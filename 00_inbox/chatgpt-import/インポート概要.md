---
title: 【廃止・記録用】ChatGPT履歴インポート概要（破損ZIP時）
type: import-report-superseded
status: failed-corrupt-zip
source: chatgpt-export-zip
created: 2026-05-17
runId: 20260517-185136
---

# 【廃止】ChatGPT履歴インポート概要（破損ZIP時の記録）

> ⚠️ **このファイルは廃止扱い。** その後ZIPが完全版（613MB）で再アップロードされ健全化したため、索引化を実施済み。
> 現行の概要は → [[00_インポート概要]]
> 本ファイルは「最初のアップロードが破損していた」という経緯の記録としてのみ残す。

---

> （以下は当時の記録）結論: 対象ZIPが破損（不完全アップロード）しており、会話本体は復旧不能だった。

---

## 1. 対象と結果サマリー

| 項目 | 内容 |
|---|---|
| 対象ZIP | `00_inbox/aaab06...5c9f0a4bd0d84732bb36ec2ed5a100f3.zip` |
| 実ファイルサイズ | 245,686,098 bytes（約 245MB） |
| 欠損バイト数 | 367,769,548 bytes（約 351MB） |
| 期待される完全サイズ | 約 613MB |
| 展開可否 | **不可（truncated / overlapped components）** |
| 会話データ | **復旧不能**（`conversations-*.json` / `chat.html` は欠損領域） |
| 救出できたファイル | `user.json` / `user_settings.json` / `export_manifest.json`（末尾メタのみ） |
| 元ZIP | **削除していない**（指示通り保持。再アップロード診断に必要） |

---

## 2. 破損の根拠（検証ログ）

- `unzip` 実行時: `missing 367769548 bytes in zipfile` / `invalid zip file with overlapped components (possible zip bomb)`
- 中央ディレクトリ（末尾の目録）は読めるが、`conversations-000.json`〜`conversations-006.json` の local header オフセットが**負値**（4バイトオフセット領域を超過＝本体データは欠損 351MB 側に存在）
- Python `zipfile` で会話JSON抽出を試行 → `OSError: [Errno 22] Invalid argument`（データ不在）
- 末尾近傍（197MB〜245MB）にある小メタデータ3件のみ抽出成功
- 推定: アップロード/転送が約 40% 地点で打ち切られた（245MB / 613MB）

---

## 3. 読み取れたファイル

| ファイル | サイズ | 内容 | Obsidian反映 |
|---|---|---|---|
| `user.json` | 165B | **PII（メール / 電話 / ユーザーID / 生年）** | ✗ 機密のため非記載（§5） |
| `user_settings.json` | 3,605B | UI設定の配列。ナレッジ価値なし | ✗ 不要 |
| `export_manifest.json` | 392KB | **ファイル対応表のみ**（path + size_bytes、1054件）。会話タイトル・本文・`title`フィールドは**含まれない** | ✗ 会話メタなし |

→ `export_manifest.json` にも会話タイトルは存在せず、ファイル名ハッシュの対応表のみ。**会話の分類・要約に使える情報は一切救出できなかった。**

---

## 4. 実施できなかったこと（理由付き）

依頼の成果物のうち以下は**作成していない**。会話内容ゼロの状態で作ると事実無根の捏造になるため、意図的に未作成とした:

- 重要会話インデックス.md / カテゴリ別まとめ.md / アプリ別まとめ.md
- 収益化アイデア一覧.md / Claude Code・Codex運用改善メモ.md / 今後やるべきToDo候補.md
- 02_apps/ 03_prompts/ 04_reviews/ 05_monetization/ 06_research/ 07_architecture/ への会話由来追記
- 重要度判定（high/medium/low）・カテゴリ別件数（元データ不在のため算出不能）

---

## 5. 機密情報チェック結果

ZIP内に**機密らしき情報あり**（内容は伏せる。Obsidianには実値を一切書かない）:

- `user.json`: メールアドレス / 電話番号 / ユーザーID / 生年 — **個人情報**
- `確定申告書類`（同名5ファイル・計約 26MB / PDF・画像）— **個人の税務・財務書類**
- `file-E8oo...detail202603(2916).csv`（8KB）— 明細CSV。**財務/取引明細の可能性**
- `scrape_2026-04-23...json`（4.6KB）— スクレイピング結果。内容未確認

対応: これら機密候補ファイルは**展開していない**（会話JSONと小メタのみ一時抽出）。実値・本文は本Markdownにも記載していない。

---

## 6. Git管理Vault（obsidian-vault）へ反映すべき候補

- **反映候補: 本「インポート概要.md」のみ**（インポート失敗の記録として履歴価値あり）
- 会話由来ナレッジは存在しないため、大量コピー対象は**なし**
- 機密候補ファイル群は obsidian-vault へ**絶対にミラーしない**

---

## 7. 次にやるべきこと

1. **ChatGPTエクスポートZIPを再取得・再アップロードする**（今回は約40%で切れている。完全版 ≈ 613MB）
   - アップロード後、`unzip -t <zip>` で整合性を先に検証してから本処理を再依頼すると安全
2. 再アップロード版が健全なら、本タスク（展開→分類→ナレッジ化）を再実行する
3. 機密ファイル（確定申告書類・user.json・明細CSV）は**エクスポートZIPに同梱しない**運用を検討（ChatGPTの「データ書き出し」はアップロード画像/添付も全て含むため、財務書類が混入している）
4. 一時展開先 `/tmp/chatgpt-import-20260517/`（メタ3ファイルのみ・約400KB）は破棄してよい

---

## 8. 未対応・不明点

- 会話本文の分類・要約・重要度判定: **すべて未対応**（元データ不在）
- 欠損領域に何件の会話があったかは不明（manifest にも会話単位の情報なし）
- `scrape_2026-04-23.json` の中身（機密該当か）は未確認（展開せず）

---

## 関連

- 一時展開先: `/tmp/chatgpt-import-20260517/`（user.json / user_settings.json / export_manifest.json のみ）
- 元ZIP: `00_inbox/aaab06...zip`（保持・未削除）
- 運用ルール: 機密ガード → `/root/company/CLAUDE.local.md` § Obsidian 要約追記ルール 機密情報ガード
